name: Build Maxed-Out Live Linux ISO

on:
  push:
    branches: [ master, main ]
  pull_request:

jobs:
  build-iso:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout kernel
        uses: actions/checkout@v4

      - name: Clone your BusyBox fork
        uses: actions/checkout@v4
        with:
          repository: wowsha/busybox   # ← CHANGE THIS ONCE
          path: busybox

      - name: Install dependencies
        run: sudo apt update && sudo apt install -y build-essential bc bison flex libncurses-dev libelf-dev libssl-dev git wget xz-utils cpio gzip xorriso mtools

      - name: Build MAXED-OUT kernel + get version
        run: |
          make defconfig
          ./scripts/config --set-str CONFIG_SYSTEM_TRUSTED_KEYS ""
          ./scripts/config --set-str CONFIG_SYSTEM_REVOCATION_KEYS ""
          yes "" | make oldconfig
          make -j$(nproc) bzImage modules

          # Save kernel version for later steps
          echo "KERNELRELEASE=$(make kernelrelease)" >> $GITHUB_ENV

      - name: Build static BusyBox (tc disabled + warnings silenced)
        run: |
          cd busybox
          make defconfig

          # Static + disable broken tc applet (the real fix)
          sed -i 's/# CONFIG_STATIC is not set/CONFIG_STATIC=y/' .config
          echo "CONFIG_TC=n" >> .config
          echo "CONFIG_FEATURE_TC_INGRESS=y" >> .config || true

          # Optional: silence all the noisy warnings you saw
          echo "CONFIG_EXTRA_CFLAGS=\"-w\"" >> .config

          make -j$(nproc) CONFIG_PREFIX=../busybox-install install

      - name: Create initramfs with ALL modules
        run: |
          mkdir -p initramfs/{bin,sbin,etc,dev,proc,sys,lib/modules/${{ env.KERNELRELEASE }}}
          cp -a busybox-install/* initramfs/

          # Copy modules from kernel source (now we're in the right dir)
          cp -r lib/modules/${{ env.KERNELRELEASE }} initramfs/lib/modules/ || echo "No modules dir yet, skipping"

          sudo mknod initramfs/dev/console c 5 1
          sudo mknod initramfs/dev/null    c 1 3
          sudo mknod initramfs/dev/random  c 1 8
          sudo mknod initramfs/dev/urandom c 1 9

          cat > initramfs/init <<'EOF'
          #!/bin/sh
          mount -t devtmpfs none /dev 2>/dev/null
          mount -t proc none /proc
          mount -t sysfs none /sys

          echo "════════════════════════════════════════════════"
          echo "  MAXED-OUT CUSTOM LINUX — ALL DRIVERS LOADED  "
          echo "  Kernel ${{ env.KERNELRELEASE }}                     "
          echo "  BusyBox from https://github.com/YOUR-USERNAME/busybox"
          echo "════════════════════════════════════════════════"
          echo

          depmod -a 2>/dev/null
          modprobe -a all 2>/dev/null || true

          exec /bin/ash
          EOF
          chmod +x initramfs/init

          cd initramfs
          find . | cpio -H newc -o | gzip > ../initramfs.cpio.gz

      - name: Create bootable ISO
        run: |
          mkdir -p iso/boot/grub
          cp arch/x86/boot/bzImage iso/boot/vmlinuz
          cp initramfs.cpio.gz iso/boot/

          cat > iso/boot/grub/grub.cfg <<EOF
          set timeout=2
          menuentry "Maxed-Out Custom Linux — Every Driver Known to Man" {
              linux /boot/vmlinuz console=ttyS0 quiet
              initrd /boot/initramfs.cpio.gz
          }
          EOF

          grub-mkrescue -o MaxedOut-Linux.iso iso

      - name: Upload ISO
        uses: actions/upload-artifact@v4
        with:
          name: MaxedOut-Linux.iso
          path: MaxedOut-Linux.iso
